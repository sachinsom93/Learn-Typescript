"use strict";
function createCourse(title, createdBy, price) {
    let newCourse = {};
    newCourse.title = title;
    newCourse.createdBy = createdBy;
    newCourse.price = price;
    return newCourse;
}
const names = ['som', 'sachin'];
